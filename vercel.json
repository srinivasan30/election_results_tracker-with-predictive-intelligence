{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install --legacy-peer-deps && npm install next@latest --legacy-peer-deps",
  "framework": "nextjs",
  "regions": ["sin1"],
  "functions": {
    "app/api/results/route.ts": {
      "maxDuration": 30
    }
  }
}
